---
phase: 01-trading-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fxsoqqabot/data/buffers.py
  - src/fxsoqqabot/data/storage.py
  - tests/test_data/test_buffers.py
  - tests/test_data/test_storage.py
autonomous: true
requirements:
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "Rolling in-memory buffers maintain fixed-size windows of recent ticks and bars per timeframe without blocking"
    - "Tick data is flushed to Parquet files partitioned by year/month for efficient analytical queries"
    - "DuckDB can query stored tick data with SQL for time-range slicing and aggregations"
    - "Trade events are stored in DuckDB for post-hoc analysis"
  artifacts:
    - path: "src/fxsoqqabot/data/buffers.py"
      provides: "Rolling in-memory buffers for ticks and bars"
      contains: "class TickBuffer"
    - path: "src/fxsoqqabot/data/storage.py"
      provides: "DuckDB/Parquet tick storage and trade event logging"
      contains: "class TickStorage"
  key_links:
    - from: "src/fxsoqqabot/data/buffers.py"
      to: "src/fxsoqqabot/core/events.py"
      via: "Buffers store TickEvent and BarEvent objects"
      pattern: "TickEvent|BarEvent"
    - from: "src/fxsoqqabot/data/storage.py"
      to: "duckdb"
      via: "DuckDB connection for analytical storage"
      pattern: "duckdb\\.connect"
---

<objective>
Build the data storage and buffering layer: rolling in-memory buffers for real-time signal computation and DuckDB/Parquet persistent storage for tick data and trade events.

Purpose: Signal modules (Phase 2) need fast access to recent data via buffers. Analytics and backtesting need historical data in Parquet.
Output: TickBuffer/BarBufferSet for in-memory access, TickStorage for DuckDB/Parquet persistence.
</objective>

<execution_context>
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-trading-infrastructure/01-CONTEXT.md
@.planning/phases/01-trading-infrastructure/01-RESEARCH.md
@.planning/phases/01-trading-infrastructure/01-01-SUMMARY.md

<interfaces>
From src/fxsoqqabot/core/events.py:
```python
@dataclass(frozen=True, slots=True)
class TickEvent:
    symbol: str; time_msc: int; bid: float; ask: float; last: float
    volume: int; flags: int; volume_real: float; spread: float

@dataclass(frozen=True, slots=True)
class BarEvent:
    symbol: str; timeframe: str; time: int; open: float; high: float
    low: float; close: float; tick_volume: int; spread: int; real_volume: int

@dataclass(frozen=True, slots=True)
class FillEvent:
    ticket: int; symbol: str; action: str; volume: float; fill_price: float
    requested_price: float; slippage: float; sl: float; tp: float | None
    magic: int; is_paper: bool; timestamp: datetime
```

From src/fxsoqqabot/config/models.py:
```python
class DataConfig(BaseModel):
    tick_buffer_size: int = 10000
    bar_buffer_sizes: dict[str, int] = {"M1": 1440, "M5": 288, "M15": 96, "H1": 24, "H4": 6}
    storage_path: str = "data/"
    parquet_partition_by: list[str] = ["year", "month"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rolling in-memory buffers for ticks and multi-timeframe bars</name>
  <files>
    src/fxsoqqabot/data/buffers.py
    tests/test_data/test_buffers.py
  </files>
  <read_first>
    src/fxsoqqabot/core/events.py (TickEvent, BarEvent dataclass definitions)
    src/fxsoqqabot/config/models.py (DataConfig -- tick_buffer_size, bar_buffer_sizes)
  </read_first>
  <behavior>
    - Test: TickBuffer(maxlen=100) stores up to 100 TickEvents, oldest evicted on overflow
    - Test: TickBuffer.append(tick) adds a TickEvent; TickBuffer.latest returns most recent
    - Test: TickBuffer.latest_n(10) returns last 10 ticks in chronological order
    - Test: TickBuffer.as_arrays() returns dict with numpy arrays for bid, ask, spread, time_msc (for signal computation)
    - Test: TickBuffer starts empty; len(buffer) == 0
    - Test: BarBuffer("M1", maxlen=1440) stores BarEvents for a single timeframe
    - Test: BarBuffer.append(bar) adds a BarEvent; BarBuffer.latest returns most recent
    - Test: BarBuffer.as_arrays() returns dict with numpy arrays for open, high, low, close, tick_volume
    - Test: BarBufferSet creates one BarBuffer per configured timeframe from DataConfig.bar_buffer_sizes
    - Test: BarBufferSet["M1"] returns the M1 BarBuffer
    - Test: BarBufferSet.update(timeframe, bars) bulk-appends a list of BarEvents
  </behavior>
  <action>
    Create src/fxsoqqabot/data/buffers.py:

    from collections import deque
    import numpy as np
    from fxsoqqabot.core.events import TickEvent, BarEvent
    from fxsoqqabot.config.models import DataConfig

    class TickBuffer:
        """Rolling fixed-size buffer of TickEvents using collections.deque(maxlen=N).
        O(1) append with automatic oldest-eviction per DATA-06."""

        def __init__(self, maxlen: int = 10000):
            self._deque: deque[TickEvent] = deque(maxlen=maxlen)

        def append(self, tick: TickEvent) -> None:
            self._deque.append(tick)

        def extend(self, ticks: list[TickEvent]) -> None:
            self._deque.extend(ticks)

        @property
        def latest(self) -> TickEvent | None:
            return self._deque[-1] if self._deque else None

        def latest_n(self, n: int) -> list[TickEvent]:
            """Return last n ticks in chronological order."""
            if n >= len(self._deque):
                return list(self._deque)
            return list(self._deque)[-n:]

        def as_arrays(self) -> dict[str, np.ndarray]:
            """Convert buffer to numpy arrays for signal computation.
            Returns dict with keys: time_msc, bid, ask, last, spread, volume_real."""
            if not self._deque:
                return {k: np.array([], dtype=np.float64) for k in
                        ("time_msc", "bid", "ask", "last", "spread", "volume_real")}
            return {
                "time_msc": np.array([t.time_msc for t in self._deque], dtype=np.int64),
                "bid": np.array([t.bid for t in self._deque], dtype=np.float64),
                "ask": np.array([t.ask for t in self._deque], dtype=np.float64),
                "last": np.array([t.last for t in self._deque], dtype=np.float64),
                "spread": np.array([t.spread for t in self._deque], dtype=np.float64),
                "volume_real": np.array([t.volume_real for t in self._deque], dtype=np.float64),
            }

        def __len__(self) -> int:
            return len(self._deque)

        def clear(self) -> None:
            self._deque.clear()

    class BarBuffer:
        """Rolling fixed-size buffer of BarEvents for a single timeframe."""

        def __init__(self, timeframe: str, maxlen: int = 1440):
            self.timeframe = timeframe
            self._deque: deque[BarEvent] = deque(maxlen=maxlen)

        def append(self, bar: BarEvent) -> None:
            self._deque.append(bar)

        def extend(self, bars: list[BarEvent]) -> None:
            self._deque.extend(bars)

        @property
        def latest(self) -> BarEvent | None:
            return self._deque[-1] if self._deque else None

        def latest_n(self, n: int) -> list[BarEvent]:
            if n >= len(self._deque):
                return list(self._deque)
            return list(self._deque)[-n:]

        def as_arrays(self) -> dict[str, np.ndarray]:
            """Returns dict with keys: time, open, high, low, close, tick_volume."""
            if not self._deque:
                return {k: np.array([], dtype=np.float64) for k in
                        ("time", "open", "high", "low", "close", "tick_volume")}
            return {
                "time": np.array([b.time for b in self._deque], dtype=np.int64),
                "open": np.array([b.open for b in self._deque], dtype=np.float64),
                "high": np.array([b.high for b in self._deque], dtype=np.float64),
                "low": np.array([b.low for b in self._deque], dtype=np.float64),
                "close": np.array([b.close for b in self._deque], dtype=np.float64),
                "tick_volume": np.array([b.tick_volume for b in self._deque], dtype=np.int64),
            }

        def __len__(self) -> int:
            return len(self._deque)

    class BarBufferSet:
        """Collection of BarBuffers, one per configured timeframe per DATA-06."""

        def __init__(self, config: DataConfig):
            self._buffers: dict[str, BarBuffer] = {
                tf: BarBuffer(tf, maxlen=size)
                for tf, size in config.bar_buffer_sizes.items()
            }

        def __getitem__(self, timeframe: str) -> BarBuffer:
            return self._buffers[timeframe]

        def update(self, timeframe: str, bars: list[BarEvent]) -> None:
            if timeframe in self._buffers:
                self._buffers[timeframe].extend(bars)

        @property
        def timeframes(self) -> list[str]:
            return list(self._buffers.keys())
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_data/test_buffers.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/data/buffers.py contains `class TickBuffer:`
    - src/fxsoqqabot/data/buffers.py contains `class BarBuffer:`
    - src/fxsoqqabot/data/buffers.py contains `class BarBufferSet:`
    - src/fxsoqqabot/data/buffers.py contains `deque(maxlen=` (O(1) eviction)
    - src/fxsoqqabot/data/buffers.py contains `def as_arrays(self) -> dict[str, np.ndarray]:`
    - tests/test_data/test_buffers.py contains tests for overflow eviction, latest_n, as_arrays
    - `uv run pytest tests/test_data/test_buffers.py -x` exits 0
  </acceptance_criteria>
  <done>
    TickBuffer and BarBuffer provide O(1) append with automatic eviction. as_arrays() converts to numpy for signal computation. BarBufferSet manages all five timeframes. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DuckDB/Parquet tick storage and trade event logging</name>
  <files>
    src/fxsoqqabot/data/storage.py
    tests/test_data/test_storage.py
  </files>
  <read_first>
    src/fxsoqqabot/core/events.py (TickEvent, FillEvent -- fields to store)
    src/fxsoqqabot/config/models.py (DataConfig -- storage_path, parquet_partition_by)
    .planning/phases/01-trading-infrastructure/01-RESEARCH.md (DuckDB/Parquet Tick Storage Schema code example)
  </read_first>
  <behavior>
    - Test: TickStorage.__init__ creates DuckDB connection and tick_data + trade_events tables
    - Test: store_ticks(ticks) inserts a batch of TickEvents into tick_data table
    - Test: store_trade_event(fill) inserts a FillEvent into trade_events table
    - Test: query_ticks(start_msc, end_msc) returns ticks within time range as pandas DataFrame
    - Test: flush_to_parquet() exports tick_data to partitioned Parquet files at configured path
    - Test: get_trade_history(limit=10) returns last N trade events as pandas DataFrame
    - Test: TickStorage works with empty database (no data yet)
    - Test: close() properly closes DuckDB connection
  </behavior>
  <action>
    Create src/fxsoqqabot/data/storage.py:

    import duckdb
    import pandas as pd
    import structlog
    from pathlib import Path
    from datetime import datetime, timezone
    from fxsoqqabot.core.events import TickEvent, FillEvent
    from fxsoqqabot.config.models import DataConfig

    class TickStorage:
        """DuckDB/Parquet storage for tick data and trade events per DATA-05.

        Tick data stored in DuckDB tables and periodically flushed to Parquet files
        partitioned by year/month for efficient time-range queries.
        """

        def __init__(self, config: DataConfig):
            self._config = config
            self._storage_path = Path(config.storage_path)
            self._storage_path.mkdir(parents=True, exist_ok=True)
            self._logger = structlog.get_logger().bind(component="tick_storage")

            self._db = duckdb.connect(str(self._storage_path / "analytics.duckdb"))
            self._init_tables()

        def _init_tables(self) -> None:
            """Create tables if they don't exist."""
            self._db.execute("""
                CREATE TABLE IF NOT EXISTS tick_data (
                    time_msc BIGINT,
                    bid DOUBLE,
                    ask DOUBLE,
                    last_price DOUBLE,
                    volume BIGINT,
                    flags INTEGER,
                    volume_real DOUBLE,
                    spread DOUBLE,
                    symbol VARCHAR
                )
            """)

            self._db.execute("""
                CREATE TABLE IF NOT EXISTS trade_events (
                    event_time TIMESTAMP,
                    ticket BIGINT,
                    symbol VARCHAR,
                    action VARCHAR,
                    volume DOUBLE,
                    fill_price DOUBLE,
                    requested_price DOUBLE,
                    slippage DOUBLE,
                    sl DOUBLE,
                    tp DOUBLE,
                    magic INTEGER,
                    is_paper BOOLEAN
                )
            """)

        def store_ticks(self, ticks: list[TickEvent]) -> int:
            """Batch insert tick events. Returns count inserted."""
            if not ticks:
                return 0
            records = [
                (t.time_msc, t.bid, t.ask, t.last, t.volume,
                 t.flags, t.volume_real, t.spread, t.symbol)
                for t in ticks
            ]
            self._db.executemany(
                "INSERT INTO tick_data VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
                records
            )
            return len(records)

        def store_trade_event(self, fill: FillEvent) -> None:
            """Insert a single trade event."""
            self._db.execute(
                "INSERT INTO trade_events VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
                [fill.timestamp.isoformat(), fill.ticket, fill.symbol, fill.action,
                 fill.volume, fill.fill_price, fill.requested_price, fill.slippage,
                 fill.sl, fill.tp, fill.magic, fill.is_paper]
            )

        def query_ticks(self, start_msc: int, end_msc: int, symbol: str = "XAUUSD") -> pd.DataFrame:
            """Query ticks in time range. Returns pandas DataFrame."""
            return self._db.execute(
                "SELECT * FROM tick_data WHERE time_msc >= ? AND time_msc <= ? AND symbol = ? ORDER BY time_msc",
                [start_msc, end_msc, symbol]
            ).fetchdf()

        def get_trade_history(self, limit: int = 100) -> pd.DataFrame:
            """Get recent trade events."""
            return self._db.execute(
                "SELECT * FROM trade_events ORDER BY event_time DESC LIMIT ?",
                [limit]
            ).fetchdf()

        def flush_to_parquet(self) -> None:
            """Export tick_data to partitioned Parquet files.
            Partitions by year/month as configured."""
            parquet_path = self._storage_path / "ticks"
            parquet_path.mkdir(parents=True, exist_ok=True)
            count = self._db.execute("SELECT COUNT(*) FROM tick_data").fetchone()[0]
            if count == 0:
                self._logger.info("no_ticks_to_flush")
                return
            # Add partition columns from time_msc
            self._db.execute(f"""
                COPY (
                    SELECT *,
                        EXTRACT(YEAR FROM epoch_ms(time_msc)) AS year,
                        EXTRACT(MONTH FROM epoch_ms(time_msc)) AS month
                    FROM tick_data
                )
                TO '{parquet_path.as_posix()}/'
                (FORMAT PARQUET, PARTITION_BY (year, month), OVERWRITE_OR_IGNORE true)
            """)
            self._logger.info("ticks_flushed_to_parquet", count=count, path=str(parquet_path))

        def get_tick_count(self) -> int:
            """Return total number of stored ticks."""
            return self._db.execute("SELECT COUNT(*) FROM tick_data").fetchone()[0]

        def close(self) -> None:
            """Close DuckDB connection."""
            self._db.close()
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_data/test_storage.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/data/storage.py contains `class TickStorage:`
    - src/fxsoqqabot/data/storage.py contains `duckdb.connect(`
    - src/fxsoqqabot/data/storage.py contains `CREATE TABLE IF NOT EXISTS tick_data`
    - src/fxsoqqabot/data/storage.py contains `CREATE TABLE IF NOT EXISTS trade_events`
    - src/fxsoqqabot/data/storage.py contains `def store_ticks(`
    - src/fxsoqqabot/data/storage.py contains `def store_trade_event(`
    - src/fxsoqqabot/data/storage.py contains `def flush_to_parquet(`
    - src/fxsoqqabot/data/storage.py contains `PARTITION_BY (year, month)`
    - src/fxsoqqabot/data/storage.py contains `is_paper BOOLEAN` in trade_events table
    - tests/test_data/test_storage.py contains tests for store_ticks, query_ticks, flush_to_parquet
    - `uv run pytest tests/test_data/test_storage.py -x` exits 0
  </acceptance_criteria>
  <done>
    TickStorage creates DuckDB tables for tick_data and trade_events. Batch tick insertion works. Trade events logged with is_paper flag. Ticks queryable by time range. Parquet flush exports with year/month partitioning. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_data/ -x -v` passes all data tests
- TickBuffer evicts oldest when full, BarBufferSet manages all 5 timeframes
- TickStorage creates DuckDB tables and writes/queries tick data
- Parquet export partitions by year/month
</verification>

<success_criteria>
- Rolling buffers provide O(1) append with numpy array extraction for signal computation
- DuckDB stores tick data and trade events with SQL queryability
- Parquet export creates partitioned files for analytical queries
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-trading-infrastructure/01-03-SUMMARY.md`
</output>
