---
phase: 01-trading-infrastructure
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/fxsoqqabot/risk/__init__.py
  - src/fxsoqqabot/risk/sizing.py
  - src/fxsoqqabot/risk/session.py
  - tests/test_risk/__init__.py
  - tests/test_risk/test_sizing.py
  - tests/test_risk/test_session.py
autonomous: true
requirements:
  - RISK-02
  - RISK-06

must_haves:
  truths:
    - "Position sizing correctly calculates lot size from equity, risk percentage, and SL distance using XAUUSD contract specs"
    - "Lot size is rounded down to broker volume_step (0.01) and clamped to volume_min/volume_max"
    - "When minimum lot (0.01) exceeds the risk budget, the trade is skipped per D-04"
    - "Three capital phases return different risk percentages: 10%, 5%, 2% per D-03"
    - "Session time filter prevents trading outside configured hours (default London-NY 13:00-17:00 UTC) per RISK-06"
  artifacts:
    - path: "src/fxsoqqabot/risk/sizing.py"
      provides: "Position sizing engine for three-phase risk model"
      contains: "class PositionSizer"
    - path: "src/fxsoqqabot/risk/session.py"
      provides: "Session time filter"
      contains: "class SessionFilter"
  key_links:
    - from: "src/fxsoqqabot/risk/sizing.py"
      to: "src/fxsoqqabot/config/models.py"
      via: "Uses RiskConfig.get_risk_pct for capital phase risk percentage"
      pattern: "self\\._config\\.get_risk_pct"
    - from: "src/fxsoqqabot/risk/session.py"
      to: "src/fxsoqqabot/config/models.py"
      via: "Uses SessionConfig.windows for trading hours"
      pattern: "self\\._config\\.windows"
---

<objective>
Build the position sizing engine with three-phase capital model and the session time filter. These are pre-trade risk gates that determine whether and how much to trade.

Purpose: Position sizing is the mathematical core of risk management. The session filter prevents trading during illiquid hours where spreads widen and false signals increase.
Output: PositionSizer for lot size calculation, SessionFilter for time-based trading control.
</objective>

<execution_context>
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-trading-infrastructure/01-CONTEXT.md
@.planning/phases/01-trading-infrastructure/01-RESEARCH.md
@.planning/phases/01-trading-infrastructure/01-01-SUMMARY.md

<interfaces>
From src/fxsoqqabot/config/models.py:
```python
class RiskConfig(BaseModel):
    aggressive_max: float = 100.0
    selective_max: float = 300.0
    aggressive_risk_pct: float = 0.10
    selective_risk_pct: float = 0.05
    conservative_risk_pct: float = 0.02
    spread_threshold_multiplier: float = 2.0

    def get_risk_pct(self, equity: float) -> float:
        if equity < self.aggressive_max: return self.aggressive_risk_pct
        elif equity < self.selective_max: return self.selective_risk_pct
        else: return self.conservative_risk_pct

class SessionConfig(BaseModel):
    windows: list[dict[str, str]] = [{"start": "13:00", "end": "17:00"}]
    timezone: str = "UTC"
    reset_hour: int = 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Position sizing engine with three-phase capital model</name>
  <files>
    src/fxsoqqabot/risk/sizing.py
    tests/test_risk/__init__.py
    tests/test_risk/test_sizing.py
  </files>
  <read_first>
    src/fxsoqqabot/config/models.py (RiskConfig with get_risk_pct method)
    .planning/phases/01-trading-infrastructure/01-RESEARCH.md (Pattern 2: Position Sizing for XAUUSD Micro-Account, Pitfall 5)
    .planning/phases/01-trading-infrastructure/01-CONTEXT.md (D-03, D-04)
  </read_first>
  <behavior>
    - Test: calculate_lot_size(equity=20, sl_distance=2.0) returns 0.01 (minimum lot)
      Calculation: risk_amount = $20 * 0.10 = $2.00, lot_size = $2.00 / ($2.00 * 100) = 0.01
    - Test: calculate_lot_size(equity=20, sl_distance=3.0) returns None (skip trade per D-04)
      Calculation: min lot 0.01 at $3 SL = $3 risk = 15% > 10% limit
    - Test: calculate_lot_size(equity=20, sl_distance=1.5) returns 0.01
      Calculation: risk_amount = $2.00, lot = $2.00 / ($1.50 * 100) = 0.0133 -> rounds to 0.01, actual risk = $1.50 = 7.5% < 10%, OK
    - Test: calculate_lot_size(equity=200, sl_distance=3.0) returns 0.03
      Calculation: risk_amount = $200 * 0.05 = $10.00, lot = $10.00 / ($3.00 * 100) = 0.0333 -> rounds to 0.03
    - Test: calculate_lot_size(equity=500, sl_distance=3.0) returns 0.03
      Calculation: risk_amount = $500 * 0.02 = $10.00, lot = $10.00 / ($3.00 * 100) = 0.0333 -> rounds to 0.03
    - Test: validate_risk_budget returns False when actual risk at min lot exceeds phase limit (D-04)
    - Test: calculate_lot_size respects volume_max cap
    - Test: calculate_lot_size with spread consideration: adjusts SL distance by adding spread
    - Test: get_capital_phase(20) returns "aggressive"
    - Test: get_capital_phase(150) returns "selective"
    - Test: get_capital_phase(500) returns "conservative"
  </behavior>
  <action>
    Create src/fxsoqqabot/risk/sizing.py:

    import structlog
    from dataclasses import dataclass
    from fxsoqqabot.config.models import RiskConfig

    @dataclass(frozen=True)
    class SizingResult:
        """Result of position sizing calculation."""
        lot_size: float
        risk_amount: float       # Dollar amount at risk
        risk_pct: float          # Actual risk as percentage of equity
        capital_phase: str       # "aggressive", "selective", or "conservative"
        sl_distance: float       # SL distance in price units
        can_trade: bool          # False if risk exceeds limit per D-04
        skip_reason: str | None  # Reason for skipping (None if can_trade)

    @dataclass(frozen=True)
    class SymbolSpecs:
        """Broker symbol specifications queried at runtime.
        Avoids hardcoding per anti-pattern guidance."""
        volume_min: float = 0.01
        volume_max: float = 100.0
        volume_step: float = 0.01
        trade_contract_size: float = 100.0  # 100 oz per lot for XAUUSD
        point: float = 0.01
        digits: int = 2

    class PositionSizer:
        """Position sizing engine implementing three-phase capital model per D-03.

        XAUUSD specifics (from Research Pattern 2):
        - Contract size: 100 oz per lot
        - 0.01 lot = 1 oz
        - Each $1 move in gold price = $1 per 0.01 lot
        - With 1:500 leverage: margin for 0.01 lot at $2000 gold = $0.40

        Key rule per D-04: If minimum lot (0.01) risk exceeds the phase limit,
        the trade is SKIPPED, not forced through.
        """

        def __init__(self, config: RiskConfig):
            self._config = config
            self._logger = structlog.get_logger().bind(component="position_sizer")

        def get_capital_phase(self, equity: float) -> str:
            """Determine current capital phase from equity per D-03."""
            if equity < self._config.aggressive_max:
                return "aggressive"
            elif equity < self._config.selective_max:
                return "selective"
            else:
                return "conservative"

        def calculate_lot_size(
            self,
            equity: float,
            sl_distance: float,     # In price units (e.g., $3.00 for a $3 SL)
            specs: SymbolSpecs | None = None,
        ) -> SizingResult:
            """Calculate lot size respecting risk budget and broker constraints.

            Returns SizingResult with can_trade=False if min lot exceeds risk limit per D-04.

            Formula: lot_size = risk_amount / (sl_distance * contract_size)
            Where risk_amount = equity * risk_pct_for_phase
            """
            if specs is None:
                specs = SymbolSpecs()  # XAUUSD defaults

            phase = self.get_capital_phase(equity)
            risk_pct = self._config.get_risk_pct(equity)
            risk_amount = equity * risk_pct

            if sl_distance <= 0:
                return SizingResult(
                    lot_size=0, risk_amount=0, risk_pct=0, capital_phase=phase,
                    sl_distance=sl_distance, can_trade=False,
                    skip_reason="SL distance must be positive"
                )

            # Calculate ideal lot size
            lot_size = risk_amount / (sl_distance * specs.trade_contract_size)

            # Round down to volume_step
            lot_size = int(lot_size / specs.volume_step) * specs.volume_step
            lot_size = round(lot_size, 2)  # Avoid floating point artifacts

            # Apply min/max clamps
            lot_size = max(specs.volume_min, lot_size)
            lot_size = min(specs.volume_max, lot_size)

            # CRITICAL per D-04: Check if actual risk at this lot size exceeds limit
            actual_risk = lot_size * sl_distance * specs.trade_contract_size
            actual_risk_pct = actual_risk / equity if equity > 0 else float('inf')

            if actual_risk_pct > risk_pct:
                # Min lot exceeds risk budget -- skip trade
                self._logger.warning("trade_skipped_risk_exceeds_limit",
                    equity=equity, phase=phase, risk_pct=risk_pct,
                    lot_size=lot_size, actual_risk=actual_risk,
                    actual_risk_pct=actual_risk_pct, sl_distance=sl_distance)
                return SizingResult(
                    lot_size=lot_size, risk_amount=actual_risk,
                    risk_pct=actual_risk_pct, capital_phase=phase,
                    sl_distance=sl_distance, can_trade=False,
                    skip_reason=f"Actual risk {actual_risk_pct:.1%} exceeds {phase} limit {risk_pct:.1%}"
                )

            self._logger.info("position_sized",
                equity=equity, phase=phase, lot_size=lot_size,
                risk_amount=actual_risk, actual_risk_pct=actual_risk_pct)

            return SizingResult(
                lot_size=lot_size, risk_amount=actual_risk,
                risk_pct=actual_risk_pct, capital_phase=phase,
                sl_distance=sl_distance, can_trade=True, skip_reason=None
            )
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_risk/test_sizing.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/risk/sizing.py contains `class PositionSizer:`
    - src/fxsoqqabot/risk/sizing.py contains `class SizingResult:`
    - src/fxsoqqabot/risk/sizing.py contains `class SymbolSpecs:`
    - src/fxsoqqabot/risk/sizing.py contains `can_trade=False` with skip logic per D-04
    - src/fxsoqqabot/risk/sizing.py contains `risk_amount / (sl_distance * specs.trade_contract_size)` formula
    - src/fxsoqqabot/risk/sizing.py contains `int(lot_size / specs.volume_step) * specs.volume_step` (round down)
    - tests/test_risk/test_sizing.py contains test for $20 equity with $3 SL returning can_trade=False
    - tests/test_risk/test_sizing.py contains test for $20 equity with $2 SL returning can_trade=True
    - tests/test_risk/test_sizing.py contains tests for all three capital phases
    - `uv run pytest tests/test_risk/test_sizing.py -x` exits 0
  </acceptance_criteria>
  <done>
    PositionSizer calculates lot size for XAUUSD across three capital phases. Returns SizingResult with can_trade flag. Skips trades when minimum lot risk exceeds phase limit per D-04. Respects broker volume constraints. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session time filter</name>
  <files>
    src/fxsoqqabot/risk/session.py
    tests/test_risk/test_session.py
  </files>
  <read_first>
    src/fxsoqqabot/config/models.py (SessionConfig -- windows, timezone, reset_hour)
    .planning/phases/01-trading-infrastructure/01-CONTEXT.md (RISK-06: London-NY overlap 13:00-17:00 UTC)
  </read_first>
  <behavior>
    - Test: is_trading_allowed() returns True at 14:00 UTC (within London-NY overlap)
    - Test: is_trading_allowed() returns False at 08:00 UTC (outside window)
    - Test: is_trading_allowed() returns False at 18:00 UTC (outside window)
    - Test: is_trading_allowed() returns True at 13:00 UTC exactly (boundary inclusive)
    - Test: is_trading_allowed() returns False at 17:00 UTC exactly (boundary exclusive -- end is exclusive)
    - Test: Multiple windows: [{"start": "08:00", "end": "12:00"}, {"start": "13:00", "end": "17:00"}] allows both ranges
    - Test: get_session_date() returns the date string for the current session (respects reset_hour)
    - Test: get_week_start_date() returns Monday's date for weekly reset
    - Test: time_until_next_window() returns seconds until next trading window opens
  </behavior>
  <action>
    Create src/fxsoqqabot/risk/session.py:

    import structlog
    from datetime import datetime, time, timezone, timedelta
    from fxsoqqabot.config.models import SessionConfig

    class SessionFilter:
        """Session time filter per RISK-06.

        Only allows trading during configured session windows
        (default: London-NY overlap 13:00-17:00 UTC).
        Also provides session date/week boundaries for circuit breaker resets per D-10.
        """

        def __init__(self, config: SessionConfig):
            self._config = config
            self._logger = structlog.get_logger().bind(component="session_filter")
            self._windows = self._parse_windows()

        def _parse_windows(self) -> list[tuple[time, time]]:
            """Parse window strings into time tuples."""
            windows = []
            for w in self._config.windows:
                start_parts = w["start"].split(":")
                end_parts = w["end"].split(":")
                start = time(int(start_parts[0]), int(start_parts[1]))
                end = time(int(end_parts[0]), int(end_parts[1]))
                windows.append((start, end))
            return windows

        def is_trading_allowed(self, now: datetime | None = None) -> bool:
            """Check if current time is within any trading window.
            Start is inclusive, end is exclusive."""
            if now is None:
                now = datetime.now(timezone.utc)
            current_time = now.time()
            for start, end in self._windows:
                if start <= current_time < end:
                    return True
            return False

        def get_session_date(self, now: datetime | None = None) -> str:
            """Return session date string (YYYY-MM-DD) respecting reset_hour per D-10.
            If current hour < reset_hour, session belongs to previous day."""
            if now is None:
                now = datetime.now(timezone.utc)
            if now.hour < self._config.reset_hour:
                session_dt = now - timedelta(days=1)
            else:
                session_dt = now
            return session_dt.strftime("%Y-%m-%d")

        def get_week_start_date(self, now: datetime | None = None) -> str:
            """Return Monday's date for the current trading week."""
            if now is None:
                now = datetime.now(timezone.utc)
            monday = now - timedelta(days=now.weekday())
            return monday.strftime("%Y-%m-%d")

        def time_until_next_window(self, now: datetime | None = None) -> float:
            """Return seconds until next trading window opens.
            Returns 0.0 if currently in a window."""
            if now is None:
                now = datetime.now(timezone.utc)
            if self.is_trading_allowed(now):
                return 0.0
            current_time = now.time()
            min_wait = float('inf')
            for start, _end in self._windows:
                if start > current_time:
                    # Window is later today
                    today_start = now.replace(hour=start.hour, minute=start.minute, second=0, microsecond=0)
                    wait = (today_start - now).total_seconds()
                else:
                    # Window is tomorrow
                    tomorrow_start = (now + timedelta(days=1)).replace(
                        hour=start.hour, minute=start.minute, second=0, microsecond=0)
                    wait = (tomorrow_start - now).total_seconds()
                min_wait = min(min_wait, wait)
            return min_wait
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_risk/test_session.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/risk/session.py contains `class SessionFilter:`
    - src/fxsoqqabot/risk/session.py contains `def is_trading_allowed(`
    - src/fxsoqqabot/risk/session.py contains `def get_session_date(`
    - src/fxsoqqabot/risk/session.py contains `def get_week_start_date(`
    - src/fxsoqqabot/risk/session.py contains `def time_until_next_window(`
    - src/fxsoqqabot/risk/session.py contains `start <= current_time < end` (start inclusive, end exclusive)
    - tests/test_risk/test_session.py tests 14:00 UTC returns True, 08:00 UTC returns False
    - tests/test_risk/test_session.py tests multiple windows
    - `uv run pytest tests/test_risk/test_session.py -x` exits 0
  </acceptance_criteria>
  <done>
    SessionFilter checks trading hours against configured windows. Default London-NY overlap 13:00-17:00 UTC. Supports multiple windows. Provides session date and week boundaries for circuit breaker resets. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_risk/ -x -v` passes all risk tests
- Position sizing returns can_trade=False for $20 equity with $3 SL (exceeds 10% limit)
- Session filter blocks trading outside configured hours
</verification>

<success_criteria>
- Position sizing calculates correct lot sizes for all three capital phases per D-03
- Trade skipped when minimum lot risk exceeds phase limit per D-04
- Lot size rounded to volume_step and clamped to min/max
- Session filter blocks trading outside configured hours per RISK-06
- Session dates respect reset_hour for circuit breaker boundaries per D-10
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-trading-infrastructure/01-05-SUMMARY.md`
</output>
