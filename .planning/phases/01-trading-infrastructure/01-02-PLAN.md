---
phase: 01-trading-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fxsoqqabot/execution/__init__.py
  - src/fxsoqqabot/execution/mt5_bridge.py
  - src/fxsoqqabot/data/__init__.py
  - src/fxsoqqabot/data/feed.py
  - tests/test_execution/__init__.py
  - tests/test_execution/test_mt5_bridge.py
  - tests/test_data/__init__.py
  - tests/test_data/test_feed.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - EXEC-01
  - EXEC-03

must_haves:
  truths:
    - "MT5 bridge wraps all blocking MT5 calls in asyncio.to_thread with a single-threaded executor to prevent concurrent MT5 access"
    - "Tick data retrieval returns TickEvent objects with bid, ask, last, volume, flags, spread"
    - "Bar data retrieval returns BarEvent objects for all five timeframes (M1, M5, M15, H1, H4)"
    - "DOM retrieval returns DOMSnapshot with entries or empty tuple when DOM unavailable, never raises"
    - "Connection drop is detected via terminal_info() checks and tick freshness monitoring"
    - "Reconnection retries indefinitely with exponential backoff per D-06"
  artifacts:
    - path: "src/fxsoqqabot/execution/mt5_bridge.py"
      provides: "Async MT5 wrapper with connection management"
      contains: "class MT5Bridge"
      exports: ["MT5Bridge"]
    - path: "src/fxsoqqabot/data/feed.py"
      provides: "Market data feed (ticks, bars, DOM)"
      contains: "class MarketDataFeed"
      exports: ["MarketDataFeed"]
  key_links:
    - from: "src/fxsoqqabot/execution/mt5_bridge.py"
      to: "MetaTrader5 package"
      via: "asyncio.to_thread wraps all mt5.* calls"
      pattern: "asyncio\\.to_thread\\(mt5\\."
    - from: "src/fxsoqqabot/data/feed.py"
      to: "src/fxsoqqabot/execution/mt5_bridge.py"
      via: "MarketDataFeed uses MT5Bridge for data retrieval"
      pattern: "self\\._bridge"
    - from: "src/fxsoqqabot/data/feed.py"
      to: "src/fxsoqqabot/core/events.py"
      via: "Feed converts raw MT5 data to TickEvent/BarEvent/DOMSnapshot"
      pattern: "TickEvent\\("
---

<objective>
Build the async MT5 bridge and market data feed: the connection layer that wraps all blocking MT5 calls for async safety, handles disconnection/reconnection, and converts raw MT5 data into typed event objects.

Purpose: Every data ingestion, order execution, and state query flows through MT5Bridge. The data feed is the live market heartbeat.
Output: MT5Bridge class with connection management, MarketDataFeed class for tick/bar/DOM retrieval.
</objective>

<execution_context>
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11/Desktop/FXSoqqaBot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-trading-infrastructure/01-CONTEXT.md
@.planning/phases/01-trading-infrastructure/01-RESEARCH.md
@.planning/phases/01-trading-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- types the executor needs -->

From src/fxsoqqabot/config/models.py:
```python
class ExecutionConfig(BaseModel):
    symbol: str = "XAUUSD"
    magic_number: int = 20260327
    deviation: int = 20
    mode: str = "paper"
    sl_atr_multiplier: float = 2.0
    mt5_path: str | None = None
    mt5_login: int | None = None
    mt5_password: str | None = None
    mt5_server: str | None = None

class DataConfig(BaseModel):
    tick_buffer_size: int = 10000
    bar_buffer_sizes: dict[str, int] = {"M1": 1440, "M5": 288, "M15": 96, "H1": 24, "H4": 6}
    tick_poll_interval_ms: int = 100
    bar_refresh_interval_seconds: int = 5
    storage_path: str = "data/"
    parquet_partition_by: list[str] = ["year", "month"]
```

From src/fxsoqqabot/core/events.py:
```python
class TickEvent:  # frozen dataclass, slots=True
    symbol: str; time_msc: int; bid: float; ask: float; last: float
    volume: int; flags: int; volume_real: float; spread: float

class BarEvent:  # frozen dataclass, slots=True
    symbol: str; timeframe: str; time: int; open: float; high: float
    low: float; close: float; tick_volume: int; spread: int; real_volume: int

class DOMEntry:  # frozen dataclass, slots=True
    type: int; price: float; volume: int; volume_dbl: float

class DOMSnapshot:  # frozen dataclass, slots=True
    symbol: str; time_msc: int; entries: tuple[DOMEntry, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async MT5 bridge with connection lifecycle and reconnection</name>
  <files>
    src/fxsoqqabot/execution/__init__.py
    src/fxsoqqabot/execution/mt5_bridge.py
    tests/test_execution/__init__.py
    tests/test_execution/test_mt5_bridge.py
  </files>
  <read_first>
    src/fxsoqqabot/config/models.py (ExecutionConfig -- mt5_path, mt5_login, etc.)
    src/fxsoqqabot/core/events.py (event types for type hints)
    .planning/phases/01-trading-infrastructure/01-RESEARCH.md (Pattern 1: Async MT5 Bridge, Pitfalls 1-3 and 7)
  </read_first>
  <behavior>
    - Test: MT5Bridge.__init__ creates ThreadPoolExecutor(max_workers=1) -- serialized MT5 access per Pitfall 2
    - Test: connect() calls mt5.initialize with kwargs from ExecutionConfig and returns True on success
    - Test: connect() returns False and logs error when mt5.initialize returns False
    - Test: ensure_connected() returns True when terminal_info is not None and .connected is True
    - Test: ensure_connected() calls connect() when terminal_info returns None (reconnection)
    - Test: get_ticks() wraps mt5.copy_ticks_from in asyncio.to_thread
    - Test: get_rates() wraps mt5.copy_rates_from in asyncio.to_thread
    - Test: get_symbol_info() wraps mt5.symbol_info in asyncio.to_thread
    - Test: get_symbol_tick() wraps mt5.symbol_info_tick in asyncio.to_thread
    - Test: get_positions() wraps mt5.positions_get in asyncio.to_thread
    - Test: order_send() calls order_check first, then order_send (per anti-pattern guidance)
    - Test: order_send() returns None when order_check fails
    - Test: shutdown() calls mt5.shutdown and shuts down executor
    - Test: last_error() wraps mt5.last_error in asyncio.to_thread
    - Test: reconnect_loop() retries with exponential backoff (1s, 2s, 4s, 8s... capped at 60s)
    Note: All tests mock the mt5 module -- no live MT5 connection needed for unit tests.
  </behavior>
  <action>
    Create src/fxsoqqabot/execution/mt5_bridge.py:

    import asyncio
    import MetaTrader5 as mt5
    from concurrent.futures import ThreadPoolExecutor
    import structlog
    from fxsoqqabot.config.models import ExecutionConfig

    class MT5Bridge:
        """Async wrapper around the blocking MetaTrader5 package.

        CRITICAL: Uses ThreadPoolExecutor(max_workers=1) to serialize all MT5 calls.
        The MT5 package uses global internal state and is NOT thread-safe (Pitfall 2).
        """

        def __init__(self, config: ExecutionConfig):
            self._config = config
            self._executor = ThreadPoolExecutor(max_workers=1, thread_name_prefix="mt5")
            self._connected = False
            self._logger = structlog.get_logger().bind(component="mt5_bridge")

        Properties: connected (bool), config (ExecutionConfig)

        async def _run_mt5(self, func, *args, **kwargs):
            """Run a blocking MT5 function in the dedicated single-thread executor.
            Checks return value and logs errors per Pitfall 1 (silent failures)."""
            loop = asyncio.get_running_loop()
            return await loop.run_in_executor(self._executor, lambda: func(*args, **kwargs))

        async def connect(self) -> bool:
            Build kwargs dict from config: path, login, password, server (only if not None).
            Call mt5.initialize(**kwargs) via _run_mt5.
            If returns False: log mt5.last_error(), return False.
            Verify terminal_info() is not None and .connected is True.
            Set self._connected = True, return True.

        async def ensure_connected(self) -> bool:
            Call terminal_info() via _run_mt5.
            If None or not .connected: log warning, set _connected=False, call connect().
            Return connection status.

        async def reconnect_loop(self, max_retries: int = 0) -> bool:
            """Retry connection with exponential backoff per D-06.
            max_retries=0 means infinite retries."""
            delay = 1.0
            max_delay = 60.0
            attempts = 0
            while max_retries == 0 or attempts < max_retries:
                attempts += 1
                self._logger.info("mt5_reconnect_attempt", attempt=attempts, delay=delay)
                if await self.connect():
                    return True
                await asyncio.sleep(delay)
                delay = min(delay * 2, max_delay)
            return False

        Data retrieval methods (all use _run_mt5):
        - async def get_ticks(symbol, date_from, count, flags=mt5.COPY_TICKS_ALL) -> numpy array or None
        - async def get_rates(symbol, timeframe, date_from, count) -> numpy array or None
        - async def get_dom(symbol) -> list or None  (calls market_book_get after market_book_add)
        - async def get_symbol_info(symbol) -> SymbolInfo or None
        - async def get_symbol_tick(symbol) -> Tick or None
        - async def get_positions(symbol=None) -> tuple or None
        - async def get_account_info() -> AccountInfo or None
        - async def terminal_info() -> TerminalInfo or None

        Order methods:
        - async def order_check(request: dict) -> OrderCheckResult or None
        - async def order_send(request: dict) -> OrderSendResult or None
          (Does NOT pre-validate here -- that's the caller's job in orders.py)
        - async def last_error() -> tuple[int, str]

        async def shutdown(self):
            Call mt5.shutdown() via _run_mt5.
            self._executor.shutdown(wait=False)
            self._connected = False

    Tests: Mock the entire mt5 module using unittest.mock.patch. Test each method independently.
    Use pytest-asyncio for async test functions.
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_execution/test_mt5_bridge.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/execution/mt5_bridge.py contains `class MT5Bridge:`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `ThreadPoolExecutor(max_workers=1`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `asyncio.to_thread` or `run_in_executor`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def connect(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def ensure_connected(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def reconnect_loop(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def get_ticks(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def get_rates(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def get_dom(`
    - src/fxsoqqabot/execution/mt5_bridge.py contains `async def shutdown(`
    - tests/test_execution/test_mt5_bridge.py mocks mt5 module (contains `@patch` or `mock`)
    - `uv run pytest tests/test_execution/test_mt5_bridge.py -x` exits 0
  </acceptance_criteria>
  <done>
    MT5Bridge wraps all MT5 calls through a single-threaded executor. Connection, reconnection with exponential backoff, data retrieval, and order methods all work. All methods handle MT5 silent failures (None returns). Tests pass with mocked MT5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Market data feed -- ticks, bars, and DOM with graceful degradation</name>
  <files>
    src/fxsoqqabot/data/feed.py
    tests/test_data/__init__.py
    tests/test_data/test_feed.py
  </files>
  <read_first>
    src/fxsoqqabot/execution/mt5_bridge.py (MT5Bridge API -- what methods are available)
    src/fxsoqqabot/core/events.py (TickEvent, BarEvent, DOMSnapshot, DOMEntry)
    src/fxsoqqabot/config/models.py (DataConfig -- poll intervals, buffer sizes)
  </read_first>
  <behavior>
    - Test: fetch_ticks() converts raw MT5 numpy array rows to list[TickEvent] with computed spread (ask - bid)
    - Test: fetch_ticks() returns empty list when MT5 returns None (silent failure handling)
    - Test: fetch_bars() converts raw MT5 numpy array to list[BarEvent] for a given timeframe string
    - Test: fetch_bars() returns empty list when MT5 returns None
    - Test: fetch_dom() converts BookInfo entries to DOMSnapshot with DOMEntry tuples
    - Test: fetch_dom() returns DOMSnapshot with empty entries tuple when market_book_get returns None or empty (graceful degradation per DATA-02)
    - Test: fetch_dom() logs a warning on first empty DOM result but does not log repeatedly (rate-limited warning)
    - Test: fetch_multi_timeframe_bars() fetches bars for all five timeframes (M1, M5, M15, H1, H4) and returns dict[str, list[BarEvent]]
    - Test: check_tick_freshness() returns False when latest tick is older than threshold (stale data detection per Pitfall 7)
  </behavior>
  <action>
    Create src/fxsoqqabot/data/feed.py:

    import numpy as np
    import structlog
    from datetime import datetime, timezone
    from fxsoqqabot.execution.mt5_bridge import MT5Bridge
    from fxsoqqabot.config.models import DataConfig
    from fxsoqqabot.core.events import TickEvent, BarEvent, DOMSnapshot, DOMEntry

    # MT5 timeframe constants mapping
    TIMEFRAME_MAP = {
        "M1": 1,    # mt5.TIMEFRAME_M1
        "M5": 5,    # mt5.TIMEFRAME_M5
        "M15": 15,  # mt5.TIMEFRAME_M15
        "H1": 16385, # mt5.TIMEFRAME_H1
        "H4": 16388, # mt5.TIMEFRAME_H4
    }
    Note: Import actual mt5 constants at runtime, fall back to numeric values for testing.

    class MarketDataFeed:
        """Retrieves and converts market data from MT5 into typed events.

        Handles:
        - Tick data polling (DATA-01)
        - DOM depth snapshots with graceful degradation (DATA-02)
        - Multi-timeframe bar data (DATA-03)
        - Stale data detection (Pitfall 7)
        """

        def __init__(self, bridge: MT5Bridge, config: DataConfig):
            self._bridge = bridge
            self._config = config
            self._logger = structlog.get_logger().bind(component="data_feed")
            self._dom_warned = False  # Rate-limit DOM empty warnings
            self._last_tick_time_msc: int = 0

        async def fetch_ticks(self, symbol: str, count: int = 100) -> list[TickEvent]:
            """Fetch recent ticks, convert to TickEvent list.
            Returns empty list on MT5 failure (per Pitfall 1)."""
            raw = await self._bridge.get_ticks(symbol, datetime.now(timezone.utc), count)
            if raw is None or len(raw) == 0:
                self._logger.warning("no_tick_data", symbol=symbol)
                return []
            events = []
            for row in raw:
                event = TickEvent(
                    symbol=symbol,
                    time_msc=int(row['time_msc']),
                    bid=float(row['bid']),
                    ask=float(row['ask']),
                    last=float(row['last']),
                    volume=int(row['volume']),
                    flags=int(row['flags']),
                    volume_real=float(row['volume_real']),
                    spread=float(row['ask'] - row['bid']),
                )
                events.append(event)
            if events:
                self._last_tick_time_msc = events[-1].time_msc
            return events

        async def fetch_bars(self, symbol: str, timeframe: str, count: int | None = None) -> list[BarEvent]:
            """Fetch bars for a single timeframe. count defaults to bar_buffer_sizes[timeframe]."""
            if count is None:
                count = self._config.bar_buffer_sizes.get(timeframe, 100)
            tf_const = TIMEFRAME_MAP.get(timeframe)
            if tf_const is None:
                self._logger.error("unknown_timeframe", timeframe=timeframe)
                return []
            raw = await self._bridge.get_rates(symbol, tf_const, datetime.now(timezone.utc), count)
            if raw is None or len(raw) == 0:
                self._logger.warning("no_bar_data", symbol=symbol, timeframe=timeframe)
                return []
            return [BarEvent(
                symbol=symbol, timeframe=timeframe,
                time=int(row['time']), open=float(row['open']),
                high=float(row['high']), low=float(row['low']),
                close=float(row['close']), tick_volume=int(row['tick_volume']),
                spread=int(row['spread']), real_volume=int(row['real_volume']),
            ) for row in raw]

        async def fetch_multi_timeframe_bars(self, symbol: str) -> dict[str, list[BarEvent]]:
            """Fetch bars for all configured timeframes (M1, M5, M15, H1, H4) per DATA-03."""
            result = {}
            for tf in self._config.bar_buffer_sizes:
                result[tf] = await self.fetch_bars(symbol, tf)
            return result

        async def fetch_dom(self, symbol: str) -> DOMSnapshot:
            """Fetch DOM depth. Returns DOMSnapshot with empty entries if unavailable per DATA-02."""
            raw = await self._bridge.get_dom(symbol)
            if raw is None or len(raw) == 0:
                if not self._dom_warned:
                    self._logger.warning("dom_unavailable_degrading_to_tick_only", symbol=symbol)
                    self._dom_warned = True
                return DOMSnapshot(symbol=symbol, time_msc=0, entries=())
            entries = tuple(
                DOMEntry(type=item.type, price=item.price, volume=item.volume, volume_dbl=item.volume_dbl)
                for item in raw
            )
            return DOMSnapshot(
                symbol=symbol,
                time_msc=int(datetime.now(timezone.utc).timestamp() * 1000),
                entries=entries,
            )

        def check_tick_freshness(self, max_age_seconds: float = 10.0) -> bool:
            """Check if the latest tick is fresh enough. Per Pitfall 7: stale data = possible disconnection."""
            if self._last_tick_time_msc == 0:
                return False
            now_msc = int(datetime.now(timezone.utc).timestamp() * 1000)
            age_seconds = (now_msc - self._last_tick_time_msc) / 1000.0
            return age_seconds < max_age_seconds

    Tests: Create mock MT5 data as numpy structured arrays. Mock MT5Bridge methods to return them.
    Test the conversion logic and edge cases (None returns, empty arrays, DOM degradation).
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Desktop/FXSoqqaBot" && uv run pytest tests/test_data/test_feed.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/fxsoqqabot/data/feed.py contains `class MarketDataFeed:`
    - src/fxsoqqabot/data/feed.py contains `async def fetch_ticks(`
    - src/fxsoqqabot/data/feed.py contains `async def fetch_bars(`
    - src/fxsoqqabot/data/feed.py contains `async def fetch_dom(`
    - src/fxsoqqabot/data/feed.py contains `async def fetch_multi_timeframe_bars(`
    - src/fxsoqqabot/data/feed.py contains `def check_tick_freshness(`
    - src/fxsoqqabot/data/feed.py contains `DOMSnapshot(symbol=symbol, time_msc=0, entries=())` (graceful degradation)
    - src/fxsoqqabot/data/feed.py contains `spread=float(row['ask'] - row['bid'])` (computed spread)
    - tests/test_data/test_feed.py contains tests for empty DOM handling
    - `uv run pytest tests/test_data/test_feed.py -x` exits 0
  </acceptance_criteria>
  <done>
    MarketDataFeed fetches ticks, bars, and DOM from MT5Bridge and converts to typed events. DOM graceful degradation returns empty DOMSnapshot. Stale tick detection works. All five timeframes fetched for multi-timeframe bars. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_execution/ tests/test_data/ -x -v` passes all tests
- MT5Bridge uses max_workers=1 executor (grep confirms)
- MarketDataFeed converts MT5 data to TickEvent/BarEvent/DOMSnapshot types
- DOM degradation returns empty tuple, logs warning once
- Tick freshness check detects stale data
</verification>

<success_criteria>
- All MT5 calls wrapped in asyncio-compatible executor with single-thread safety
- Reconnection retries indefinitely with exponential backoff per D-06
- Tick, bar, and DOM data fetched and converted to typed event objects
- DOM graceful degradation per DATA-02 -- never crashes on missing DOM
- Multi-timeframe bar support for M1/M5/M15/H1/H4 per DATA-03
- All tests pass with mocked MT5
</success_criteria>

<output>
After completion, create `.planning/phases/01-trading-infrastructure/01-02-SUMMARY.md`
</output>
